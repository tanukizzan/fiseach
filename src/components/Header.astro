<header>
  <button class="login-button" disabled>ログイン</button>
</header>

<style>
  header {
    display: flex;
    justify-content: flex-end;
    width: 100%;
  }
  header .login-button {
    color: var(--bg-color);
    font-weight: bold;
    text-decoration: none;
    margin: 1em;
    padding: 0.6em 0.9em;
    border-radius: 6px;
    background-color: var(--fisea-blue);
    border: none;
  }
  header .login-button:disabled {
    background-color: var(--fisea-blue);
    color: var(--bg-color);
    opacity: 0.3;
  }
</style>